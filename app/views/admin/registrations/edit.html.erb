<section class="content-header">
  <h1>Configurações <small>Perfil</small></h1>
</section>

<section class="content">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, role: :form }) do |f| %>

  <div class="row">
    <div class="col-md-6">

      <div class="box box-primary">
        <div class="box-header">
          <h3 class="box-title">Perfil</h3>
        </div>
        <div class="box-body">

          <div class="form-group">
            <label for="user_name">Nome</label>
            <%= f.text_field :name, placeholder: 'Email', class: 'form-control' %>
          </div>

          <div class="form-group">
            <label for="user_email">Email</label>
            <%= f.email_field :email, placeholder: 'Email', class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :password, 'Nova senha' %> <i>(deixe em branco se você não quiser alterar)</i>
            <%= f.password_field :password, class: 'form-control', autocomplete: "off" %>
          </div>

          <div class="form-group">
            <%= f.label :password_confirmation, 'Confirme a nova senha' %>
            <%= f.password_field :password_confirmation, class: 'form-control', autocomplete: "off" %>
          </div>

        </div>

      </div>
    </div>

    <div class="col-md-6">
      <div class="box box-primary">
        <div class="box-header">
          <h3 class="box-title">Social</h3>
        </div>

        <div class="box-body">
          <%#= f.fields_for :socials do |s| %>
            <% collection_for[:socials].each do |social| %>
            <div class='form-group'>
              <label><%= social %></label>
              <%#= s.hidden_field :network, value: social %>
              <div class="input-group input-group-lg half-input">
                <div class="input-group-btn">
                  <%#= s.text_field :url, class: 'form-control' %>
                </div>
              </div>
            </div>
            <% end %>
          <%# end %>
        </div>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-md-12">
      <div class="box-footer">
        <%= f.submit "Atualizar", class: 'btn btn-default' %>
      </div>
    </div>
  </div>

  <% end %>

</section>
